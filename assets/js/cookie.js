lang.init({

  cookie: {
    // cookie name
    name: 'langCookie',
    //  expiration <a href="https://www.jqueryscript.net/time-clock/">date</a>
    expiry: 365,
    // path
    path: '/'
  },

  // If true, cookies will override the "currentLang" option if the cookie is set.
  allowCookieOverride: true

});
